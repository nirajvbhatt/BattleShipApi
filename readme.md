# Prerequisites:
dotnet 5 or above installed (check with dotnet --version)
VS Code with C# extension

# Overview
This App contains BattleShipApi with a limited capability of Adding a Ship (Horizontally or Vertically only) on a Game Board of 10,10.
There is an API endpopnt

# Improvements:
1) Add a test project
2) Add cloudwatch logging configuration
3) Integrate with DynamoDB to manage Game state
4) Deploy API to ECS
5) Add CloudFormation template to create required AWS resources and create CI/CD pipeline using CodeBuild and CodePipeLine
